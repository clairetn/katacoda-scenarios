The **mkdir** command (**m**a**k**e **dir**rectory) create a new repository:

`mkdir path_for_new_directory`

The **rmdir** (**r**e**m**ove**dir**ectory) command is used to delete an empty directory:

`rmdir anEnptyDir `

